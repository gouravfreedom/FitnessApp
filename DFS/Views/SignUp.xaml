<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="DFS.Views.SignUp">
    <ContentPage.Content>
        
        <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" BackgroundColor="{StaticResource BackColor}">
            <StackLayout VerticalOptions="StartAndExpand" Margin="10, 30, 10, 10">
                
                <Label Text="Email address" TextColor="White"  />
                <Entry x:Name="emailEntry" Placeholder="Email Address" />
                <Label Text="Password" TextColor="White" />
                <Entry x:Name="passwordEntry" IsPassword="true" Placeholder="Password" />
                <Label Text="Confirm Password" TextColor="White" />
                <Entry x:Name="confirmPasswordEntry" IsPassword="true" Placeholder="Confirm Password" />

                <Button Text="Sign Up" HeightRequest="30" BackgroundColor="Red" TextColor="White" Clicked="OnSignUpButtonClicked" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
